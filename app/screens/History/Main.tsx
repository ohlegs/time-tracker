import React from 'react';
import { View, Text } from 'react-native';
import { BG_MAIN } from '../../helper/colors';

export default function History() {
  return (
    <View style={{ height: '100%', width: '100%', backgroundColor: BG_MAIN }}>
      <Text>History</Text>
    </View>
  );
}
